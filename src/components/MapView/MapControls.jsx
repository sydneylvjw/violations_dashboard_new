// these are the features to render map controls like zoom buttons and scale indicator etc

export default function MapControls() {
  return (
    <div className="map-controls">
      <strong>Map Controls</strong>
      <div>(You can add basemap toggles, reset zoom, etc.)</div>
    </div>
  );
}